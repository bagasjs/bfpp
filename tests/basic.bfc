34 35 add dbgprint ;; This should be 69
80 11 sub dbgprint ;; This should be 69
1  2  sub dbgprint ;; This should be 256
20 20 eq dbgprint  ;; This should be 1
20 19 eq dbgprint  ;; This should be 0
20 20 neq dbgprint  ;; This should be 0
20 19 neq dbgprint  ;; This should be 1

37 dup dbgprint dbgprint ;; This should become 37, 37
10 20 swap dbgprint dbgprint ;; This should become 20, 10, but it will print 10, 20
10 37 over dbgprint dbgprint dbgprint ;; This should become 10, 37, 10

